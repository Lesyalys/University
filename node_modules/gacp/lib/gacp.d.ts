import { EMOJI_TYPES } from './configs';
declare type Hooks = {
    postpush: string;
};
export default function gacp({ cwd, add, push, emoji, editor, verify, hooks, }: {
    cwd: string;
    add: boolean;
    push: boolean;
    emoji: EMOJI_TYPES;
    editor: boolean;
    verify: boolean;
    hooks: Hooks;
}): Promise<void>;
export {};
