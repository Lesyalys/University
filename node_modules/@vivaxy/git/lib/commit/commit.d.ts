/**
 * @since 2019-06-28 09:59
 * @author vivaxy
 */
import { Stdio } from '../helpers';
export default function commit(message: string, { cwd, noVerify, stdio }: {
    cwd: string;
    noVerify?: boolean;
    stdio?: Stdio;
}): Promise<void>;
