import { Stdio } from '../helpers';
export default function push({ cwd, followTags, setUpstream, noVerify, stdio, }: {
    cwd: string;
    followTags?: boolean;
    setUpstream?: boolean;
    noVerify?: boolean;
    stdio?: Stdio;
}): Promise<void>;
