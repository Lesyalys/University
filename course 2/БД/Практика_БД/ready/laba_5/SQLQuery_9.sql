--9. Ожидается, что через 20 лет население мира вырастет на 10 процентов.
--Вывести список континентов с прогнозируемым населением:DECLARE @WORLD FLOAT = 1.0SELECT C.Континент,SUM(C.Население)  AS 'Население',FLOOR(SUM(C.Население)) AS 'Население 10%'FROM Страны AS C GROUP BY C.Континент 